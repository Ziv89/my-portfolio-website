import { useNavigate } from "react-router-dom";

export const navigateToPortfolio = (navigate) => {
  navigate("/Portfolio");
};


export const navigateToExperience = (navigate) => {
  navigate("/Experience");
};

export const navigateToAbout = (navigate) => {
  navigate("/About");
};

export const navigateToContact = (navigate) => {
  navigate("/Contact");
};